import sys
from pyfiglet import Figlet
import random

text = input("Input: ")
if len(argv) == 2:
    if 
    try:
        f = Figlet(font=argv[2])
    except ValueError:
        sys.exit()

    print("Output: ")
    print f.renderText(text)